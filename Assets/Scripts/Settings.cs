using System;
using UnityEngine;

namespace Amnesia.Settings {
    public static class Settings {
        // Int is a Dropdown(DropdownAttribute) or Slider(RangeAttribute)
        // Float is a slider
        // String is an InputField

        public static float MouseSensitivity = 2;
    }
}
